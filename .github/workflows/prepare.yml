name: Prepare and Build

on:
  workflow_call:
    inputs:
      config_file:
        description: "Path to the JSON config file"
        required: true
        type: string
      feature_set:
        description: "Feature Set"
        required: false
        type: string
      ksu_branch:
        description: "KernelSU branch or commit (empty = latest dev)"
        required: false
        type: string
        default: ""
      susfs_commit:
        description: "SUSFS commit hash (empty = latest on auto-derived branch)"
        required: false
        type: string
        default: ""
      target_os_patch_level:
        description: "Build only this OS patch level (e.g. 2024-05); empty = full date matrix"
        required: false
        type: string
        default: ""

jobs:
  generate-matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.final.outputs.matrix }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7

      - name: Read Config and Build Final Matrix
        id: final
        shell: bash
        run: |
          if [ ! -f "${{ inputs.config_file }}" ]; then
            echo "Error: Config file not found: ${{ inputs.config_file }}"
            exit 1
          fi

          CONFIG_JSON=$(cat "${{ inputs.config_file }}" | jq -c .)

          # Build { "include": [ {android_version:..., kernel_version:..., sublevel:..., os_patch_level:..., variant: ...}, ... ] }
          # Always include Normal, and add any explicit per-item variant from the config.
          # When target_os_patch_level is set, keep only the matrix entries for that
          # one date — lets a single run build one specific kernel instead of the
          # whole monthly matrix, for fast iteration/testing.
          FINAL=$(jq -cn \
            --argjson cfg "$CONFIG_JSON" \
            --arg target_date "${{ inputs.target_os_patch_level }}" '
            ($cfg.version | split("-")) as $version_parts
            | {
              include: (
                [
                  $cfg.include[] as $item
                  | {
                      version: $cfg.version,
                      android_version: $version_parts[0],
                      kernel_version: $version_parts[1],
                      sublevel: $item.sublevel,
                      os_patch_level: $item.date,
                      variant: "Normal"
                    }
                ]
                + [
                  $cfg.include[] as $item
                  | select($item.variant != null)
                  | {
                      version: $cfg.version,
                      android_version: $version_parts[0],
                      kernel_version: $version_parts[1],
                      sublevel: $item.sublevel,
                      os_patch_level: $item.date,
                      variant: $item.variant
                    }
                ]
                | unique_by([.android_version, .kernel_version, .sublevel, .os_patch_level, .variant])
                | if ($target_date | length) > 0 then map(select(.os_patch_level == $target_date)) else . end
              )
            }')

          MATRIX_COUNT=$(echo "$FINAL" | jq '.include | length')
          if [ "$MATRIX_COUNT" -eq 0 ]; then
            echo "::error::No matrix entries match target_os_patch_level='${{ inputs.target_os_patch_level }}' for $(echo "$CONFIG_JSON" | jq -r .version) — check ${{ inputs.config_file }} for valid dates."
            exit 1
          fi
          echo "Matrix has $MATRIX_COUNT entries after filtering."

          echo "matrix=$FINAL" >> "$GITHUB_OUTPUT"

  build:
    needs: generate-matrix
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.generate-matrix.outputs.matrix) }}
    uses: ./.github/workflows/build.yml
    with:
      version: ${{ matrix.version }}
      android_version: ${{ matrix.android_version }}
      kernel_version: ${{ matrix.kernel_version }}
      sublevel: ${{ matrix.sublevel }}
      os_patch_level: ${{ matrix.os_patch_level }}
      variant: ${{ matrix.variant }}
      feature_set: ${{ inputs.feature_set }}
      ksu_branch: ${{ inputs.ksu_branch }}
      susfs_commit: ${{ inputs.susfs_commit }}
    secrets: inherit